@model IEnumerable<SitioPC.Models.productos>
@{
    ViewBag.Title = "productos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="p-2 text-white">
    <h2 class="text-center pt-3 pb-4">Carrito de compras</h2>
    <table class="table table-hover table-responsive-sm table-striped table-light table-borderless table-sm">
        <tr>
            <th>
                @*Html.DisplayNameFor(model => model.UserName)*@
                <label>Nombre</label>
            </th>
            <th><label>Precio</label></th>
            <th><label>Cantidad</label></th>
            <th><label>Subtotal</label></th>
            <th><label>Img</label></th>
            <th>

            </th>
        </tr>


        <tr>
            <td>
            </td>
            <td> </td>
            <td> </td>
            <td> </td>
            <td> </td>

            <td>
                @*Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })*@
                @Html.ActionLink("Quitar", "", null, new { @class = " text-decoration-none btn-sm btn-outline-danger" })

            </td>
        </tr>
    </table>
</div>